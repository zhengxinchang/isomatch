## Changes

- Update the README.md to add the installation instructions.
- Add a new tool `isomatch tools valtable` to extract per-transcript attribute values from source GTFs and assemble them into a matrix aligned to the merged GTF transcript order.
